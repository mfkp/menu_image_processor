<h1>Menus</h1>

<table>
  <tr>
    <th>Menu</th>
    <th>Delete</th>
  </tr>

<% @menus.each do |menu| %>
  <tr>
    <td><%= link_to menu.path, menu %></td>
    <td style="text-align:center;"><%= link_to '[x]', menu, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<div class="pagination">
  <%= will_paginate @menus, :container => false %>
</div>